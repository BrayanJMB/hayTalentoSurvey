@model IEnumerable<ProyectoIdentity.Models.ModelsJourney.Encuesta>

@{
    ViewData["Title"] = "Index";
}
@section Styles{
    <link href="~/lib/datatables.net-bs4/dataTables.bootstrap4.min.css" rel="stylesheet"/>
}

<h1>Mis Encuestas</h1>

<p>
    <a asp-action="Create">Nueva Encuesta</a>
</p>
<table class="Display" id="TableEncuestas">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreEncuesta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescripcionEncuesta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Company)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreEncuesta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionEncuesta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Company.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Modificar</a>
                <a asp-action="Details" asp-route-id="@item.Id">Detalles</a> 
            </td>
        </tr>
}
    </tbody>
</table>

@section Scripts{
    <script src="~/lib/datatables.net/jquery.dataTables.min.js"></script>
    <script src="~/lib/datatables.net-bs4/dataTables.bootstrap4.min.js"></script>
    <script>
        $(document).ready( function () {
            $('#TableEncuestas').DataTable(
                {
                    "language": {
                        "url": "//cdn.datatables.net/plug-ins/1.10.15/i18n/Spanish.json"
                    }
                }
            
            );

        } );
    </script>
}
